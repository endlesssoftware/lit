# Suite configuration.  Evaluated as Perl with ($config, $lit_config) in
# scope; see the README for the full API.

$config->name('demo');
$config->suffixes('.test');

$config->test_source_root($config->dir);
$config->test_exec_root($lit_config->param('build', $config->dir . '/_out'));

# A feature the tests can select on.
$config->add_feature('demo-feature');

# Substitutions are plain text replacement; the key may also be a qr//.
$config->add_substitution('%greet', 'echo hello from the demo suite');
